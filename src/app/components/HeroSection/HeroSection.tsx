import styles from "./HeroSection.module.css";
const HeroSection = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.title}>Rulltrappor & Hissar</h2>
    </div>
  );
};

export default HeroSection;
